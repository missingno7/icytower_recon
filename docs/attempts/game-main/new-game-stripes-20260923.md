# `new_game` source correction

Original `new_game` at `0x40dc9c` makes five consecutive `movl $0` writes at offsets 67, 77, 87, 97, and 107 to `bg_stripe_ids[4]` through `[0]`, followed by `last_stripe_y` at 117. The previous active source cleared `cmdline.jumps/combos/sd/keys/tiny` at those sites, which is a wrong data-object mapping despite matching opcode shapes.

The isolated `takeover-new-game-stripes-20260923` current-order probe replaced only those writes: 58/82 main functions stayed exact, no losses, and `new_game`'s first difference moved from offset 69 to 127. Production was corrected and a fresh whole-CU verification kept all 58 matches.

The original then compares `profile->best_floor` to 999 *before* integer division, sets `floors.max` to 9 or the quotient, compares that value to `profile->start_floor`, and writes `floors.value` once. The earlier source divided first, capped later, and could write `floors.value` twice. The isolated `takeover-new-game-floor-value-20260923` probe compiled to 1,132/1,139 bytes with no exact-neighbor losses; original and candidate instructions at offsets 142–179 now agree in operation and register choice except for the one-byte shift from the earlier `itrcheck` load. Production was updated and the fresh all-CU gate again retained 58/82 main exact and 204 strict function matches project-wide.

The first remaining difference is offset 127: `mov itrcheck,%eax` in the original versus `%esi` in the candidate. This is a register/context investigation, not proof that changing the downstream witness or adding a dummy operation is justified. The two source/probe snapshots and failed states are retained; `new_game` remains `DIFFER`.

A later `takeover-new-game-replay-guard-20260923` probe removed the candidate's `if (!demo) return 0` immediately after `create_replay(64000)`. Original offsets 887–923 show the call, assignment, profile handle load, and `strcpy` without an intervening test. The production body now follows that path; it compiles to 1,124/1,139 bytes and still first differs at offset 127. A fresh all-CU gate again retained 58/82 exact in `main.c` and 204 exact overall. This is source correction, not a status promotion.

The `takeover-new-game-player-pointer-20260923` probe removed the candidate's extra `Tplayer *p` local and reloaded `ply[player_id]` for each write after `reset_player`. Original and candidate instruction shapes and registers now align at offsets 442–532. The isolated current-order unit kept 58/82 exact functions with no losses; `new_game` compiled to 1,136/1,139 bytes and still first differed at 127. Production was updated and verified again.

The `takeover-new-game-crash-path-20260923` probe removed the unsupported `return 0` after a failed `create_game_data()`. Original offsets 1117–1134 call `log2file`, reload `gameData`, and jump back to the replay assignment; they do not return. The isolated probe kept all 58 exact neighbors. Production now follows that path and compiled to 1,135/1,139 bytes in the fresh all-CU verification, which retained 204 exact game functions overall. The remaining first difference at 127 still needs investigation; neither change promotes `new_game` to a match.

Original offset 725 branches directly to the epilogue with the zero result of `load_frames`, so the earlier `return 1` on failure was wrong. `takeover-new-game-load-frames-return-20260923` changed it to zero in a current-order probe: 1,139/1,139 bytes, 58/82 exact neighbors, no losses. Production was corrected and freshly verified. The original instruction length and return path now align, while the first production-order mismatch remains offset 127.

The `takeover-new-game-historical-combined-20260923` probe compiled that source in historical order with the retained `draw_frame` and `play` candidates. In that context, the `itrcheck` register at offset 127, `got_joystick` at 755, and the custom-game test at 928 aligned with the original. The probe exposed three substantive remaining differences: `jumpSequence` writes went upward rather than downward by field offset; the original address at both offsets 338 and 928 is `is_playing_custom_game` (`0x4dd188`), while the candidate used `gdLastJumpDiff` and `itrcheck`; and the original recording branch at 1055 jumps past the scroll-counter resets, while the candidate executed them on both paths.

The `takeover-new-game-jump-order-20260923` probe changed the writes to `num`, `dist`, `start` and moved the first difference to offset 340. The `takeover-new-game-state-20260923` probe also corrected the two `is_playing_custom_game` references and moved `scroll_count`/`scroll_delay` into the existing-replay branch. It reports `new_game` as strict `FUNCTION_MATCH`, 1,139/1,139 bytes, with complete relocation and direct-target agreement, at historical emission position 64. Its object SHA-256 is `9d0dfaba25e2f2c88ec20f940fe506d78f396b49aab69698a0987727291d4e2d`; the overlay and compiler identities are in the probe record. The whole-unit probe has 61/82 exact functions, gaining `new_game` and three others but losing `run_demo`; it is diagnostic and cannot replace the production baseline. The active source incorporates the original-backed corrections. Fresh `--verify-all` retains 204 exact functions including 58/82 in `main.c`, while production-order `new_game` remains `DIFFER` at offset 127.
