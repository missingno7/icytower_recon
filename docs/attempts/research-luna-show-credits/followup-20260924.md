# `show_credits` optimizer follow-up (2026-09-24)

## Strict status and saved outputs

Current focused card remains `DIFFER`: 654 candidate bytes vs 634 historical, first raw byte difference at offset 26 is an unresolved `.rdata` address relocation. This first raw byte is not the loop verdict. Prior production-equivalent full-TU controls preserve 63/82 exact functions, with no gains or losses; historical definition order also left `show_credits` and the CU match count unchanged. No new full-TU probe was warranted here because the saved runs already discriminate the available TU-order and source-loop hypotheses.

The retained effective-outcome grouping reports 9 probes and 4 unique outcomes, all `DIFFER`:

- `7f11e57156718292`: 654 bytes, five probes (baseline, explicit-order, goto CFG, historical order, update-clause family).
- `6e95560190333763`: 658 bytes, two guarded-do variants.
- `d7faf308e7a55624`: 627 bytes, ledger body 8.
- `2fb3a1b2ea0a1231`: 624 bytes, ledger body 9.

These outputs are retained under `docs/attempts/tu-context/game-main/luna-show-credits-*.json`; use `python tools/effective_outcomes.py game-main show_credits --pattern "luna-show-credits-*.json" --compact` to reproduce the grouping. Source-loop families were not repeated.

## Original CFG and DWARF facts

The original executable disassembly at `0x40c4dc..0x40c758` is retained in the local shell output from `objdump.exe -d --start-address=0x40c4dc --stop-address=0x40c75a assets/icytower15.exe` (read-only). After initialization, control jumps to the single Esc / `cycle_count <= 149` test at `0x40c6a3`. On the active path it snapshots `cycle_count`, checks menu focus and audio, waits until the counter changes, exits if `closeButtonClicked`, subtracts `vol_step`, then falls through to that one guard. The original therefore emits one static guard block, reached first by the entry jump and again after each body iteration.

The focused evidence confirms matching relevant types and interfaces: `vol` and `vol_step` are `double`; `gc` is signed `int`; `logoBMP` is `BITMAP *`; `cycle_count` is `volatile int`; `closeButtonClicked` is `int`; Allegro `key` is `volatile char[127]`. The function signature is `void show_credits(void)`. Relevant direct calls include `checkMenuFocus`, `adjust_sample`, and `rest`; existing code keeps those calls out of line. The function is at historical and candidate emission position 48, after exact `show_instructions` in both.

## Locked GCC boundary

Existing pass dumps were generated with unchanged `-O2 -g -mfpmath=387`; their object identities equal the ordinary TU probe objects. In baseline `003t.original` and `065t.tailr2`, the loop has one guard and `vol = vol - vol_step` before it. At `066t.ch`, GCC 4.4.1 loop header copying clones the Esc/count guard into the entry path (guard count rises from one to two). At `085t.sink`, GCC reports sinking the subtraction from the old update block behind the cloned guard. The final extra test and displaced subtraction follow those two passes.

This is the concrete blocker: with the recovered types, call signatures, source body, and locked flags, the candidate reaches the right one-guard CFG before `ch`; the historical machine output retains that pre-`ch` shape. Current/historical definition order and the tested local loop forms do not alter the `ch` → `sink` path. No current interface or shared-type mismatch has evidence that would change the pass input.

## Handoff

Strict body status stays `DIFFER`; no exact neighbors were changed. The next discriminating artifact is historical compiler invocation/preprocessed source (or equivalent build configuration) that establishes why the original skipped/differed at `ch`. A speculative `-fno-tree-ch`, volatile change, or body rewrite is not established evidence and should not be promoted as a recovery.

Detailed earlier source/pass experiment notes remain in `docs/attempts/research-luna-show-credits/README.md`; compiler pass snapshots are in its `compiler-dumps/` directory; focused evidence is `docs/current/function-evidence/main/show_credits.json`.

