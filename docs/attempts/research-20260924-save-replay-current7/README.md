# `save_replay` probe on the promoted 7/15 replay TU

Date: 2026-09-24. Isolated locked TDM-2 / GCC 4.4.1 `-O2` research. No maintained source, current generated state, or ledger was edited.

## Control and hypotheses

Control: current `src/replay.c` after the `load_replay` promotion, with its seven exact functions (`get_sort_method`, `set_sort_method`, `hash`, `destroy_replay`, `update_file_list`, `load_replay`, `get_replay_property`). Original DWARF gives `save_replay` prototype `int save_replay(const char *, const char *, Treplay *, int, int)`, locals `PACKFILE *pf`, `int i`, `time_t my_time`, `struct tm *timer`, automatic `char *months[12]`, `char filename[2048]`, `int cs`, and inner-scope `Treplay *tmp` under `else`. The retained complete typed-handle body applies these type/scope facts without changing any statements.

The direct-call census is 38 calls on each side. Both same-CU calls (`calc_replay_checksum` and `load_replay`) resolve to the independently identified historical/candidate function entries. Every named external call and 41 of 42 relocations resolve equal. The only unresolved item is the `.rdata` DIR32 at body +16: candidate addend 896, original operand `0x4d7da0`, reported `section base not independently established`. The section-base constraint report has no independent groups, but a separate full-section scan below independently identifies a unique months table on each side. Do not use the original immediate itself to find that table.

## Adaptive batch result

The batch tested the typed control and two diagnostics that moved only local declaration order (`filename` before `months`; `i` before `pf`). All three compile, retain all seven exact functions with no losses, and produce one deduplicated effective output (`5ad9c18b4765a468`). Each remains strict `DIFFER`, 1227/1227 bytes, first mismatch +16, 7 differing bytes, and 41/42 equal relocations. The two code-only register-choice residuals are instruction bytes at offsets 80/83 and 1042/1045; they differ in register encodings while decoded control/targets remain comparable. No tested body is gate-ready.

The typed-handle candidate's strict report independently confirms exact same-CU targets and the full call/relocation set. The initial body mismatch list is `[16, 17, 18, 80, 83, 1042, 1045]`; the separate data scan now establishes the owner of +16, leaving only four register-encoding byte differences. Effective-output grouping means the tested declaration-order edits are not useful next steps. The prior owner-context experiment's `save_replay` match is a different TU context and is not transferable to this 7/15 maintained branch.\n\n## Independent months-table owner check\n\n`scan_months_table.py` scans the complete original PE `.rdata` section and candidate COFF `.rdata` section on its own; its search does not read the `save_replay` code operand. It recognizes contiguous 12-pointer tables whose pointees are exactly `Jan` through `Dec`, and finds exactly one table in each complete section. Original is `.rdata + 0x3da0` (section offset 15776; pointer values point to the 12 adjacent month strings at `.rdata + 0x3d5b` through `+0x3d87`). Candidate is `.rdata + 0x380` (addend 896; its 12 COFF DIR32 entries at `0x380` through `0x3ac` target candidate month strings at `+0x34f` through `+0x37b`). The candidate function relocation at `save_replay +16` names `.rdata + 0x380`; only after discovering the unique original table independently did I compare its address against the original instruction operand `0x4d7da0`. Thus table identity is independently constrained. The two section-relative offsets differ, as expected for a COFF object contribution versus linked PE section.\n\nA source-backed ordering hypothesis was also tested: compile the same typed body with `--order historical`, derived from original DWARF source lines, while holding the current seven exact functions as strict preservation requirements. It preserves all seven, but `save_replay` remains `DIFFER` and groups into the same `5ad9c18b4765a468` effective result. No new source-order fix was found. The unresolved item is therefore verifier recognition of this independently located table owner; after owner adjudication, four instruction bytes remain different at offsets 80/83 and 1042/1045 (the `make_new_date` test and `tmp` null-check register choices). No gate-ready body is established.

## Artifacts

- Batch source variants and manifest: this directory (`typed-control.c`, `filename-before-months.c`, `i-before-pf.c`, `batch-manifest.json`).
- Strict focused receipts: `../tu-context/game-replay/save-replay-current7-typed-control-20260924.json`, `save-replay-current7-decl-order-filename-months-20260924.json`, `save-replay-current7-decl-order-i-pf-20260924.json`.
- Strict comparison for typed control: `build/tu-context/game-replay/save-replay-current-typed-handle-20260924/comparison.json`.
- Independent data scan: `scan_months_table.py` and the evidence above. Strict comparison still reports the relocation as unresolved; do not change the ledger or source based only on this diagnostic report.
