{
  "remove_top_level": ["get_profile_dir_for_profile"],
  "includes_after": {"allegro.h": ["directories.h"]},
  "evidence": "Isolated shared-interface hypothesis: replace profile.c's duplicate extern int(... unsigned int buflen ...) with the maintained directories.h declaration int(... size_t buflen ...). Current MinGW DWARF reports size_t as unsigned int, so the source spelling differs while the i386 type layout is equal. No maintained source/header changed."
}
