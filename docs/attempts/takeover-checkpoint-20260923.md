# Reconstruction checkpoint — 2026-09-23

## Verified baseline

- Checkout: `main` at `dc62a0ad25e16566fd142c59fc44b7950bdd9b48` when work began. Twenty-one `docs/experiments/*.json` files were already modified; none was overwritten intentionally or staged here.
- Oracle fixture SHA-256: `7570c6b0c7cddf6180d7c421bdc7d7bc1486c47a6d62cc6fde90670f62d4388d`. TDM-2 lock SHA-256: `dfde67e60d23238d9489b5898bd3ebf758d6ebb871721cc16804b49c93bd9b22`; pinned `gcc.exe`: `13ce95bd196a45f232c99acca1fd1ef81c0db3d5937349e4beafb8724a006986`.
- `python tools/refresh_recovery.py --verify-all` freshly compiled and compared all 25 game-tree CUs. It reproduced 204 `FUNCTION_MATCH`, 3 `CODEGEN_SIMILAR`, 46 `DIFFER`, and 33,186/116,113 strictly verified game-function bytes. `game-main` was 58/82. `python tools/check_function.py game-timer fps_counter` reproduced a 45/45-byte strict function match with zero relocation mismatches. `timer.c` has three exact functions and complete 152-byte text, but `object_match` is false.
- `python tools/audit.py` passed fixture, census, toolchain, upstream, and generated-ledger invariants. The ordinary source link succeeded but included two known synthetic gameplay replacements; it does not establish recovery. `python tools/recovered_game_link.py --diagnostic` now produces a separately named, explicitly incomplete executable. No full-EXE equality has been claimed or tested.

## Fresh main.c context experiment

Command: `python tools/tu_context_probe.py game-main src/main.c takeover-20260923 --order historical --body draw_frame=docs/attempts/game-main/draw_frame-merged.c --body play=docs/attempts/game-main/play-merged.c --focus draw_frame --focus play --focus run_demo --focus stopGameMusic --no-dumps`.

Record: `docs/attempts/tu-context/game-main/takeover-20260923.json`; compiled overlay object SHA-256 `3b2dbedb55a3e4d71c5ba1d7d59de4ea4062b69596385c452a0ad3f8b524f60e` (the overlay source and object identities are in the record). The generated order puts `draw_frame` at historical position 41, `play` at 78, then `run_demo` at 79; 79/82 functions have their historical predecessor. The probe gains `draw_progress_bar`, `log2file`, and `open_web_browser`, loses `run_demo`, and changes code in many untouched bodies. It is not a promotable TU state. `draw_frame` is 8,506/8,518 bytes and `play` is 16,863/17,420; all observed historical direct call edges are present, but neither body matches. The first differences at offset 8 are stack allocations (draw: `0x1dc` versus `0x1cc`; play: `0x9fc` versus `0xa3c`), while aligned instruction comparison shows broader control-flow/code-generation differences. Do not target the peephole cursor or `run_demo` directly.

## Corrective changes and remaining work

- `function_data_refs.py` now uses actual PE section ranges. It finds previously omitted `play` globals at `0x4bc020` (`hasFocus`), `0x4bc024` (`lastFocus`), `0x4bc0c0` (`hints`), `0x4bc174` (`checkMusicVoiceID`), and `0x4bc17c` (`start_speeds`). It distinguishes an empty string at `0x4d4bb3`, known objects, null pointers, and unknown references.
- `unused_locals.py` ignores candidate comments and strings, preserves distinct DWARF locals, and reports location attributes. Its output is an investigation lead, not behavioral proof. `play` has nine unmentioned DWARF locals in the current retained candidate; several have no surviving location attribute.
- Gross source deficits now outrank a first stack-frame difference in `classify_diff.py`, so production `draw_frame` and `play` are routed as `SOURCE_INCOMPLETE`. Their body candidates remain separate. The recovered-game link/stage path refuses incomplete source; diagnostic linking remains available.
- A bounded `change_profile` task was opened and aborted without editing source. Original offset 13 jumps to shared `select_profile` setup at offset 76 using `eax`; current code emits a null-path `xor edx,edx` at offset 192 and a jump back. The same mismatch remains in historical TU order. Failure is recorded in `docs/attempts/game-main/change_profile.jsonl`. Do not repeat cosmetic condition rewrites without a new caller/declaration hypothesis.
- The active `main_menu_callback` is a strong but incomplete candidate containing a known synthetic Welcome back text slice; the source itself flags the missing original rank/version presentation. It is recorded separately from the two wholesale synthetic bodies in the provenance manifest.
- The current main dependency task is to recover the missing source/control-flow shape in the retained `draw_frame` and `play` bodies, verify all instructions and data references, then test them together in historical TU context. `draw_frame`'s early floor/stripe region and `play`'s game-over/result blocks have the largest localized differences. Use `tools/line_budget.py` only as a locator; exact comparison and relocation checks remain the acceptance gate.

## Resume commands

`python tools/audit.py`; `python -m unittest discover -s tools -p test_recovery_diagnostics.py -v`; `python tools/recovered_game_link.py --diagnostic`; and the combined probe command above. Re-run `python tools/refresh_recovery.py --verify-all` after meaningful source or verifier changes so the current ledger and link receipt are fresh.

## Later work in this run

After committing the diagnostic corrections as `08dba07f`, the original menu rank/version strings, four rank draws, cursor selection, two fixed-point head bitmap planes, and face-reset ordering were reconstructed in a separate `main_menu_callback` body. The latest historical-order combined probe compiles it to 3,775/3,741 bytes with every original direct call edge present; the original frame allocation and initial instructions now align through offset 72, but it is still `DIFFER`, and the unit still regresses `run_demo`. See `docs/attempts/game-main/main-menu-rank-20260923.md` and the adjacent TU probe records. Production source and recovery statuses remain unchanged by this experiment.

After `c92b2502`, production `new_game` was corrected from invented `cmdline` zeroing to the original `bg_stripe_ids[4..0]` writes and from divide-then-cap to the original cap-then-divide floor setup. The first mismatch moved to the `itrcheck` load at offset 127; size is 1,132/1,139. Fresh `--verify-all` retained 204 strict matches, including 58/82 in `main.c`, with no loss. See `docs/attempts/game-main/new-game-stripes-20260923.md`. The source is improved but the function remains `DIFFER`.

The original `create_replay` path was then checked instruction by instruction: it contains no null-return guard before the profile-name copy. The unsupported candidate guard was removed and verified in a no-loss whole-TU probe and fresh `--verify-all`. Current `new_game` size is 1,124/1,139 and first differs at offset 127. The `main.c` exact count is still 58/82.

Two more original-backed corrections followed. After `reset_player`, the original reloads `ply[player_id]` for the four player writes rather than retaining the candidate's extra local pointer. On failed `create_game_data()`, the original logs and continues to the replay assignment rather than returning zero. Separate current-order TU probes for each change kept all 58 exact neighbors. Both corrections are now in production source; fresh `python tools/refresh_recovery.py --verify-all` retained 204 strict function matches, including 58/82 in `main.c`. `new_game` remains `DIFFER` at 1,135/1,139 bytes, first difference offset 127 (`itrcheck` loaded into `esi` rather than original `eax`). Probe records are `docs/attempts/tu-context/game-main/takeover-new-game-player-pointer-20260923.json` and `takeover-new-game-crash-path-20260923.json`. Next investigate upstream declarations/call context and other semantic differences rather than changing the downstream register witness.

Latest source transaction: original offset 725 returns the zero result of failed `load_frames`; offsets 184–204 clear `jumpSequence.num/dist/start` in descending address order; original relocation target `0x4dd188` at offsets 338 and 928 is `is_playing_custom_game`; and the recording branch at 1055 skips scroll-counter resets. These were corrected in the active `new_game` body. Current-order source still reports `DIFFER` at offset 127, 1,139/1,139 bytes, with 58/82 exact `main.c` functions and 204 exact project-wide after fresh `--verify-all`. The isolated historical-order probe with both retained `draw_frame` and `play` candidates, `docs/attempts/tu-context/game-main/takeover-new-game-state-20260923.json`, reports strict `FUNCTION_MATCH` for `new_game` with all relocations and direct targets resolved, but regresses `run_demo`. That probe has 61/82 exact functions and is not integrated. Next investigate the actual upstream `draw_frame`/`play` and translation-unit context needed to preserve `run_demo` while retaining this `new_game` match. Do not promote production status from the isolated probe.

Follow-up `play` probes isolate the `run_demo` regression to the retained `play` body in historical order; order alone and `draw_frame` alone preserve it. The retained `play` candidate now initializes its rank/name-entry state before the loop, tests the original `done` slot for zero, and exits the results animation when `hy <= 140.0f` instead of using an unsupported shake-counter predicate. Current combined historical-order candidate size is 16,852/17,420 bytes; `new_game` remains a strict match in that probe and `run_demo` remains different. Production source, canonical statuses, and the ordinary link gate are unchanged. See `docs/attempts/game-main/play-control-flow-20260923.md` and the seven `takeover-new-game-{order-only,draw-only,play-only}`/`takeover-play-{rank-setup,rank-combined,height-exit,height-combined}` probe records. Next inspect other name-entry predicates and the play control-flow gaps with original instruction evidence.

The original third-initial branch writes 19 to the name-entry counter. An isolated literal correction made no `play` instruction-byte or relocation change, though its full object changed in debug/source contributions; branch reachability is not proven by that observation. The original branch at offset 13862 reaches `readkey` when that counter equals 20; the candidate used the opposite condition. After correcting both in the retained `play` candidate, `takeover-play-readkey-guard-20260923` compiles to 16,989/17,420 bytes (previously 16,852), with `new_game` still exact in the historical-order overlay and `run_demo` still different. The combined `draw_frame`/`play` probe confirms the same statuses. No production or canonical status change. Next inspect original name-entry branches 12878–13009 and 13833–13879, then return to the remaining `play` gaps and the resulting compiler context.

Latest branch trace: original 12878–12912 sends the guest/high-score/classic/recording case to 13833. Other paths decrement `done` (unless 20) at 12918 before polling controls; then the guest/high-score/custom path calls `keypressed` and, when `done == 20`, writes 14 at 13732. The retained candidate has the decrement after polling and does not write 14. `python tools/line_budget.py game-main play --order historical --body play=docs/attempts/game-main/play-merged.c --top 15 --lines 4918-4923 --annotations --label takeover-play-counter-branch-budget-20260923` reports 18/12/13-byte deficits on lines 4918/4922/4923. These are locators, not proof of a particular C nesting. The next actionable experiment is a coupled reconstruction of this name-entry CFG in the separate play candidate, preserving the current production baseline.

The coupled counter-path experiment is now in the retained `play` candidate. Historical-order play-only probes compile it to 17,183/17,420 bytes, versus 16,989 before, while preserving isolated `new_game` as `FUNCTION_MATCH` and leaving `run_demo` different. Two source nestings gave identical `play` instruction bytes and relocations; their full COFF hashes differ due to source/debug information, so source-spelling variation at this condition is exhausted. Probe records now include `compiled_object_identity`; the older `object_identity` field names the verified production baseline object. All 29 takeover probes were backfilled only after their saved overlay source hashes were rechecked against the records. The next discriminating work is the surrounding gamepad/keyboard branch topology, not this counter expression or the downstream `run_demo` witness.

The name-entry CFG investigation then established that `done` (stack `-0x938`), `skip_keys` (`%edi`), `pos` (`%esi`), and `alpha_pos` (stack `-0x930`) are distinct. The candidate had merged these and omitted the input delay. The original branch at 12912 reaches the keyboard/gamepad path at 13833, which rejoins the common `done` decrement at 12918; the custom-game path uses both `keypressed` and `is_fire` to arm `done=14`. The retained `play` candidate now reflects these observed paths, the letter scan, fire branches, and alphabet wrap. Latest combined historical-order probe: `takeover-play-rejoin-20260923`, `play` 17,358/17,420 and still `DIFFER`, isolated `new_game` strict `FUNCTION_MATCH`, `run_demo` still `DIFFER`, 61/82 exact functions. Production source and 204 project-wide strict matches are unchanged. The next useful experiment is instruction/relocation comparison in the name-entry region or investigation of larger `play` deficits outside it. See `docs/attempts/game-main/play-control-flow-20260923.md`; do not treat byte-size closeness as proof.

Commit `d9072912` captured that `play` candidate and the corrected probe object identities. The next `draw_frame` investigation found an actual renderer-call mismatch: original offset 4889 calls Allegro `rect` through vtable `+0xbc`, while the retained candidate called `rectfill` through `+0x3c`; its bar y coordinates were also 10 too large. The separate candidate now uses `rect` and the original `cx+0x14`/`cx+0x13` values. A vtable-call inventory then exposed an omitted `draw_sprite_h_flip` arm at original offset 8237. The old candidate explicitly left it out to remain near the target size and duplicated the overlay block for the same reason. The candidate now has the observed arm in one shared block and matches original vtable-call counts, but the whole function is still `DIFFER` at 8,175/8,518 bytes. `takeover-draw-shared-flip-20260923` is the current combined probe: 61/82 exact, isolated `new_game` exact, `run_demo` different. See `docs/attempts/game-main/draw-frame-bar-20260923.md`. Next investigate the original and candidate instruction/relocation structure around lines 2606–2638, without restoring size-driven duplicate code.

Commit `902f882d` captured the draw candidate and probes. Next, original `play` direct-call counts found seven `new_rand` sites versus the candidate's six. Offset 16822's `gotHigh` path copies the personal-records text and still reaches guest/hint selection. The exclusive candidate branch was corrected in `docs/attempts/game-main/play-merged.c`; `takeover-play-summary-branch-20260923` compiles to 17,354/17,420 and still has six random call sites. Explicitly duplicating the C guest/hint branches in `takeover-play-summary-duplicated-20260923` also produced six sites, so it was reverted. Preserve both probes. The next discriminating step is upstream control-flow/declaration analysis of the summary path, rather than more cosmetic C duplication. Production remains 204 strict matches, `main.c` 58/82, with no integration of either large candidate.

After commit `3f13192e`, a focused `draw_frame` audit found that original offsets 1952, 3406, and 6066 write `1` to `%esi` in the DWARF `p_im` ranges; the current candidate's earlier claim that `p_im=1` had no instruction support was too broad. The unsupported part was a guessed `custom.frame[0]` null guard. No assignment has been added yet because its governing predecessors and lifetime remain to be established. Original and candidate each have 64 direct calls, and virtual-call slot counts now agree. Continue from the original CFG around 1857–1962, 3365–3416, and 6026–6076, then compare the candidate code in historical TU context.

Commit `7cc66bb3` captured that provenance correction. The next data-reference audit found `play` offset 11278 `fsubrs 0x4d6da0` was incorrectly reported as an empty string: the bytes are float `130.0f`. `tools/function_data_refs.py` now recognizes x87 memory operand widths before testing NUL-terminated strings; focused tests cover this real PE address and the legitimate empty string. The retained `play` easing target was corrected from `136.0` to original `130.0f` (`takeover-play-panel-target-20260923`). Its line 3583 threshold was corrected from float `0.01f` to original double `0.01`; `takeover-play-x87-threshold-20260923` reproduces the original local x87 instruction pattern. Combined probe remains 61/82 exact, `play` `DIFFER` 17,358/17,420, isolated `new_game` exact, `run_demo` different. Production source/status remains unchanged. Next inspect remaining unresolved data references and source/relocation topology, especially the two random-hint blocks, without treating unresolved section-based relocations as definite mismatches.

Commit `f8ba3d46` captured the x87 correction. The saved `takeover-play-x87-threshold-20260923` object also contains unique exact byte sequences for original `speeds[9]`, `key_flag[7]`, and `letters[31]`; hashes and PE ranges are in `docs/attempts/game-main/play-control-flow-20260923.md`. This is data-content evidence, not a relocation or object match. Only pre-existing `docs/experiments/*.json` modifications and untracked `docs/attempts/game-timer/` remain outside committed work.

Commit `bce988f4` captured that array-content evidence. Original `play` offset 11240 reuses the `-0x928` counter as `%esi` `falling` in the results animation. The candidate's separate provisional counter and extra results-loop `140.0f` test were removed, leaving the original two `140.0f` references. The original F1-release poll and Tab/Shift `rest(2)` wait were restored. The latest combined historical-order probe, `takeover-play-results-waits-20260923`, reaches **62/82 strict functions with no losses**, including both `new_game` and unchanged `run_demo`; `play` remains `DIFFER` at 17,501/17,420 and `draw_frame` remains `DIFFER` at 8,175/8,518. Isolation shows the Tab wait alone restores `run_demo`, while the F1 wait alone does not. Production remains 204 strict game functions and `main.c` 58/82. Next inspect the remaining results-loop instructions and other `play` source gaps, then integrate only as a whole verified TU transaction. Do not promote a status from these probes.

The replay epilogue contained an extra candidate `play_sound` call because the debug and menu paths spelled it twice. Original line 5011 has one shared call: debug is checked before the menu; after the menu, recording is rechecked. The separate candidate now captures that pre-menu debug value and shares the call. `takeover-play-epilogue-debug-snapshot-20260923` compiles `play` at 17,468/17,420 and keeps both downstream `run_demo` and `new_game` strictly exact in the combined historical-order probe (62/82, no losses). `play` and `draw_frame` remain `DIFFER`. Isolation source regeneration and hashes are documented in `docs/attempts/game-main/play-control-flow-20260923.md`. Next compare exact result/epilogue instructions and relocation targets; the remaining byte excess is diagnostic, not a reason to remove observed operations.

Commit `26ef3798` captured the results-loop and replay-epilogue candidates. Subsequent original instruction checks corrected three more waits in the retained `play` candidate: initials-entry F1 release at line 4804, initials-entry Shift+Tab `rest(2)` loop at line 4932, and gameplay F1 release before measuring screenshot pause at line 4065. The debug frame branch at lines 4360–4363 was also reversed in the candidate; original Shift+Tab busy-waits for `cycle_count>7` and ordinary debug waits with `rest(2)` for nonzero cycle. Latest `takeover-play-debug-cycle-wait-20260923` combined historical-order probe: 62/82 strict, `new_game` and unchanged `run_demo` exact, no losses, `play` `DIFFER` 17,481/17,420, `draw_frame` `DIFFER`. Current production still has 204 strict game functions and `main.c` 58/82; do not promote from candidate. See `docs/attempts/game-main/play-control-flow-20260923.md` for exact original offsets and commands. Next resolve remaining semantic and relocation differences in `play`, especially duplicated random-hint call site and stack/local lifetime, then integrate with `draw_frame` only as a verified TU transaction.

The `draw_frame` predecessor trace found original `p_im=1` writes at 1952/3406/6066. An initial trial coupled each to a nearby `ply->frame=0` reset; it was reverted after the 6066 path proved unconditional after the frame-range check. Original `test %esi; je` at 2631 enters the edge-sprite block when `p_im==0`, and edge sprite calls skip the ordinary rotate/frame draw by jumping to 2819. The candidate had inverted that condition and drawn through both routes. `takeover-draw-p-im-exclusive-edge-20260923` tested the exclusive source shape: `draw_frame` remained `DIFFER` 8,181/8,518, `play` remained `DIFFER` 17,481/17,420, and the combined historical-order unit stayed 62/82 exact. The status-zero path at original 2256–2329 is not yet modeled fully. Production remains unchanged. See `docs/attempts/game-main/draw-frame-bar-20260923.md`.

Correction to the preceding provisional inference: original 6030's `jle` jumps past the overflow reset to 6051, then still writes `p_im=1` at 6066. Thus the trial assignment inside `if (frame > 3)` was misplaced; all trial `p_im=1` additions were reverted while preserving the independently supported exclusive edge route. Latest `takeover-draw-exclusive-no-guess-p-im-20260923` compiles `draw_frame` to 8,153/8,518, still `DIFFER`, with combined 62/82 strict, `new_game`/`run_demo` exact. The three physical writes and status-zero bypass must be solved as one CFG. No production status changed; previous probes remain as failed experiment evidence.

Original line 2597's x87 `jbe` edges skip the frame reset at `sx>=0.2` and `sx<=-0.2`. The candidate had reset outside that band. `takeover-draw-frame-speed-band-20260923` changes it to the open inside band, producing `draw_frame` `DIFFER` 8,145/8,518 with the combined 62/82 strict set unchanged. Original status-zero path at 2256–2308 tests the narrower `(-0.02,0.02)` band and can branch directly to the edge sprite; this remains absent from the candidate. Next recover that path and the downstream `p_im=1` routes as one control-flow reconstruction, then address remaining codegen/layout. Production remains 204 strict project-wide, `main.c` 58/82.

A separate `draw-frame-status-zero-trial.c` modeled the narrow status-zero edge shortcut and outer speed route. `takeover-draw-status-zero-route-trial-20260923` compiles at 8,327/8,518 and keeps the 62 exact combined functions. Its source duplicates both `±0.02` literal relocations (two each versus one each in the original), so do not integrate it on size grounds. The retained candidate remains `draw_frame` `DIFFER` 8,145/8,518 in `takeover-draw-frame-speed-band-20260923`; production status is unchanged.

The `play` results arrays were declared too early relative to the historical DWARF block: the original block starts at line 4650, after the replay filename buffer scope. Moving its opening brace and declarations there in `play-merged.c` changes the candidate frame from `0xa2c` to `0xa1c`; the original is `0x9fc`. The candidate's `speeds` and `rec_ctrl` stack starts now match original `-0xac` and `-0xd0`, and its `qualify`/`qualifyValue`/`fbuf` DWARF locations match original fbreg offsets. `takeover-play-results-scope-retained-20260923` still reports `play` `DIFFER` 17,495/17,420, `draw_frame` `DIFFER` 8,145/8,518, and 62/82 combined strict with `new_game`/`run_demo` exact. The residual 32 byte frame excess includes a spill in the candidate's line 4299 where original retains the value in `%ecx`; investigate upstream types/control flow and historical TU context, not an artificial spill fix. Production remains 204 strict project-wide and `main.c` 58/82.

The second status-zero draw trial moved the outer speed route past the `p_im` range test into the downstream assignment block. `takeover-draw-status-zero-join-trial-20260923` still duplicates the `±0.02` literal relocations, with `draw_frame` `DIFFER` 8,326/8,518 and unchanged 62/82 combined exact functions. The previously cited four `mov $1,%esi` instructions are the candidate's `1 - custom.frame[0]->h` computation, not identified `p_im` writes; that opcode-count inference is retracted. The original trial input was restored from commit `f7b76a0b`; the join trial is frozen separately so each JSON probe points at a source file with its recorded SHA-256. The retained draw candidate remains the speed-band version (8,145/8,518). Next recover the shared narrow-band comparison and status/pose predecessor topology.

The next instruction/data-reference check resolved the `±0.02` duplication: the original status-zero path alone uses `±0.02`, while pose bases 5–7 use strict `±0.01` directly and include zero speed. The retained `draw_frame` candidate's extra outer `±0.02` and `sx!=0` guards were removed, and the status-zero near-still edge shortcut plus downstream `p_im=1` route were integrated after a separate probe. Latest `takeover-draw-status-zero-pose-retained-20260923`: `draw_frame` `DIFFER` 8,254/8,518; `play` `DIFFER` 17,495/17,420; 62/82 strict in combined historical order, with `new_game` and `run_demo` exact and no losses. Candidate/original now each reference the six `±0.02`, `±0.01`, `±0.2` doubles once, and virtual-call slot counts match. This is still incomplete source and not a production promotion. Earlier status-zero trials remain frozen as failures of the old pose guard; do not repeat them without new evidence. Next compare actual CFG and relocation targets in `draw_frame` around lines 2594–2638; do not optimize by byte size.

Latest locator: original `draw_frame` references `custom.frame[0]` six times (five plain height loads, one indexed lookup), while retained candidate has five (four height loads, one indexed). `draw-frame-signed-speed-trial.c` split the `±0.2` speed tests according to the original sign branch; `takeover-draw-signed-speed-trial-20260923` still has four candidate height loads and gives `draw_frame` `DIFFER` 8,253/8,518, so that spelling was not integrated. Preserve the trial; next investigate the predecessor CFG/live-register differences that lead GCC to duplicate the original height-load block. Retained candidate stays 8,254/8,518, production stays 204 strict game functions with `main.c` 58/82.

An edge predecessor correction supersedes the earlier claim that edge==1 enters the overlay. Original edge==0 branches to offset 3115; edge==1's sprite call at 2494 and edge==2's flipped call at 3552 both jump to post-draw 2819. The retained candidate used `if(edge!=2)` after `if(edge)`, drawing the overlay a second time for edge==1. `takeover-draw-edge-zero-exclusive-20260923` makes the overlay the exclusive `else` route. Latest combined probe remains 62/82 strict with `new_game` and `run_demo` exact, no losses; `draw_frame` is still `DIFFER` 8,216/8,518, `play` still `DIFFER` 17,495/17,420. Virtual-call slot counts remain exact. Production source and canonical statuses remain unchanged. Next trace the missing `custom.frame[0]` height-load predecessor and remaining mismatches using relocations and CFG.
