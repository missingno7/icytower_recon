# `load_character_bmp` BBRO trace

Research only. The source and generated status were not changed.

The original instruction/line table proves the historical open-failure block was laid out at +0x56d before the [datafile] parser. Candidate GCC trace formation places the same cold block at the function tail. Rewriting success nesting was output-identical. The pass trace explains candidate placement, but available DWARF and call/data evidence do not identify a source spelling, branch-probability fact, or historical CFG construction that makes GCC select the original placement. Do not continue free-form syntax variations.

See `status.json` for exact inputs, strict status, retained peers, and pass evidence.
