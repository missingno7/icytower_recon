# do_replay_menu isGuest normalization batch

## Predictions recorded before compilation

The focused current card first differs at +397 (VA 0x411125), directly adjacent to the historical `isGuest` branch feeding the shared `strcpy` pointer join. Compare maintained `!stricmp(...)` initialization with `stricmp(...) == 0` and an equivalent explicit 0/1 conditional.

If a variant changes register choice, frame allocation, branch topology, or first mismatch, boolean normalization can affect this early code and remains live. If all three have the same effective output, GCC canonicalizes these forms in current context and the local expression family closes. Strict status and exact-peer effects are recorded separately.

All variants compile as body overlays in the full historical `game-main` TU, with no prototypes and current production source as base.
