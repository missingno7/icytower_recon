# `play` name-entry and results-loop control flow

The current production `play` body is synthetic and gated from recovered-game linking. This experiment changes only the retained evidence candidate `docs/attempts/game-main/play-merged.c`; it does not promote or integrate `play`.

Three current-source historical-order probes isolate the `run_demo` regression. `takeover-new-game-order-only-20260923` and `takeover-new-game-draw-only-20260923` keep `run_demo` as `FUNCTION_MATCH`. `takeover-new-game-play-only-20260923` changes its unchanged body and reports `DIFFER`, as does the combined `draw_frame`/`play` probe. The changed register at `run_demo` offset 52 and the one-byte shift at 73 are a downstream compiler-context witness; no `run_demo` body edit is justified.

Original `play` offsets 12015–12093 call `get_rank_id`, compute `(rank_id + 0x4a) << 4`, initialize the name-entry cursor, rank Y, scroller Y, and write 20 to the `done` stack slot before entering the loop at offset 12121. At offset 12108 the loop tests that slot for zero. The candidate initialized the counter to zero before the loop, tested it against 20, and put the other setup writes inside the later rank-change branch at original offset 12333. `takeover-play-rank-setup-20260923` moved the setup before the loop and changed the exit test to zero. The historical-order `play` candidate compiled to 16,699/17,420 bytes; `new_game` remained a strict function match and `run_demo` remained different. The candidate was 16,767 bytes before this experiment. The smaller size is not a reason to restore the unsupported control flow.

Original offsets 11863–11880 load the float literal at `0x4d6d84` (read-only PE bytes `00 00 0c 43`, exactly `140.0f`), compare it with local `hy` at `-0x934(%ebp)`, and jump back while `hy > 140.0f`. The candidate's `ply[player_id]->shake == 0 && falling > 0` exit was unsupported. `takeover-play-height-exit-20260923` replaces it with `hy <= 140.0f`; the candidate becomes 16,852/17,420 bytes. `takeover-play-height-combined-20260923` confirms that in the current combined historical-order context, `new_game` still matches strictly and `run_demo` still differs. No production recovery status changed.

The exact commands are recorded in the probe labels and can be rerun with `python tools/tu_context_probe.py game-main src/main.c <label> --order historical --body play=docs/attempts/game-main/play-merged.c --focus play --focus new_game --focus run_demo --no-dumps`, adding the retained `draw_frame` body for combined probes. Old candidate bytes are preserved by the preceding commit `a69609d7`; every probe record includes source, object, and compiler identities. Next inspect the other name-entry predicates and the results-loop branch topology before working on historical source shape or the downstream register witness.

The next trace found that original offset 17365 writes 19, not 20, when the third initial is entered. `takeover-play-typed-counter-20260923` changed this literal. Its compiled overlay object differs from `takeover-play-height-exit-20260923` because the source/debug information differs, but decoded `play` instruction bytes and relocations are identical; this does not by itself prove whether the branch is unreachable. The original `cmpl $0x14` at offset 13862 jumps to `readkey` at 17216 when the counter equals 20; the candidate's nested branch required it to differ from 20. `takeover-play-readkey-guard-20260923` corrected this condition. The resulting `play` code changed and grew from 16,852 to 16,989 bytes, still 431 short of the original. The combined probe `takeover-play-readkey-combined-20260923` again keeps isolated `new_game` exact but regresses `run_demo`; production status remains untouched. Further work should inspect the surrounding `poll_control`/`keypressed` branches at original offsets 12878–13009 and 13833–13879, including the write of 14 to the same counter at 13732. Do not vary the downstream `run_demo` body or the already measured counter literal without new upstream evidence.

That surrounding control flow is now bounded more precisely. Original offsets 12878–12912 divert the `isGuest && gotHigh && !is_playing_custom_game && recording` case to the `poll_control`/`keypressed` path at 13833; every other path reaches offset 12918, which decrements `done` only when it is not 20, *before* `poll_control` at 12936. After that call, offsets 12956–12981 test `isGuest`, `gotHigh`, and `is_playing_custom_game`: the custom-game path calls `keypressed` at 12983, then writes 14 to `done` at 13732 when it was 20. The retained candidate currently puts the decrement after `poll_control` in an `else` arm and lacks this write of 14. A line-budget probe for 4918–4923 shows deficits of 18, 12, and 13 bytes on the relevant lines. The next experiment should reconstruct these branches together from the original CFG, not add a standalone write or chase the byte deficit.

`takeover-play-counter-cfg-20260923` moved that decrement before polling, excluded the original diverted branch, and added the custom-game `keypressed`/write-14 path in the separate candidate. `play` grew to 17,183/17,420 bytes without changing the 57/82 exact-function count of the isolated play-only historical-order unit; `new_game` stayed strictly exact and `run_demo` stayed different. `takeover-play-counter-cfg-nested-20260923` expressed the same condition as nested tests. Its `play` instruction bytes and relocations were identical, though the full object hash differed because of source/debug information. Stop varying this local condition's C spelling. The remaining problem is the larger input-branch topology, including which path reaches gamepad input and `readkey`.

The input-topology probe moved the `readkey` path to the original branch reached at offset 13833 when `isGuest && gotHigh && !is_playing_custom_game && recording`. This is the branch from original offset 12912; the other path runs through the `done` decrement at 12918 and `poll_control` at 12936. That candidate compiled to 17,132/17,420 bytes. The first separated-state probe then exposed a more fundamental error: original offset 12071 zeroes `%edi` (`skip_keys`), 12073 zeroes `%esi` (`pos`), and 12075 stores 20 at stack `-0x938` (`done`). Stack `-0x930` is `alpha_pos`. The old candidate used one `skip_keys` variable as `done`, omitted the actual input cooldown, and used `alpha_pos` for both alphabet selection and initials position. `function_lines.py game-main play --blocks` confirms the distinct DWARF names and locations; instruction operands at 13875–14151 and 15822–16169 confirm their roles.

`takeover-play-separate-input-state-20260923` separates those variables and reconstructs the `%edi` countdown, yielding 17,339/17,420 bytes. `takeover-play-single-branch-20260923` places the `done` decrement inside the original else branch, yielding 17,364/17,420. `takeover-play-fire-cfg-20260923` replaces a guessed `is_fire` arm with the branches at original offsets 14059–14098 and 17385–17420, yielding 17,382/17,420. `takeover-play-key-scan-20260923` replaces a synthetic `matched` flag with the observed alphabet scan and separate `alpha_pos`/`pos` writes, yielding 17,324/17,420. The shorter output is retained because the recovered control flow and variable identities are stronger evidence than code size. All four combined historical-order probes compile, keep isolated `new_game` strictly exact, and leave `run_demo` different. `play` remains `DIFFER`; neither its size nor this note is a promotion claim.

Further original jumps resolve the custom-game route: `keypressed()` at 12983 and `is_fire(&ctrl)` at 13707 jointly gate the write of 14 at 13732. `takeover-play-custom-fire-20260923` adds that observed call. The scan at 17261–17375 continues after a matching letter, writes the found index to `alpha_pos`, and sets `skip_keys` to 100; the old candidate's early `break` was unsupported. At 13982 the compare preserves `alpha_pos` through `len` and wraps only when greater, consistent with the left-arrow wrap to `len`. `takeover-play-key-loop-bounds-20260923` records both corrections.

The gamepad path's jumps at 13925/13945 return to 12918. Thus the keyboard/gamepad branch then reaches the `done` decrement and second `poll_control` call in the same frame; the prior exclusive `else` was wrong. `takeover-play-rejoin-20260923` records the corrected fallthrough. Its historical-order candidate is 17,358/17,420 bytes, still `DIFFER`, with isolated `new_game` exact and `run_demo` different. Do not infer correctness from the near size. The current candidate still needs instruction-level comparison throughout the name-entry loop and the other `play` regions. The production `play` body and recovery status are unchanged. Full COFF hashes are recorded as compiled-overlay identities but include debug/source differences; compare decoded instructions and relocations when judging source-shape experiments.

Direct-call multiplicity exposed another source-flow error: the original has seven `new_rand` call sites, while the candidate had six. Original offsets 16822–16945 copy `"New personal records!    "` when `gotHigh`, then continue to the guest-message or random-hint selection; the candidate used an exclusive `else if`, skipping that selection on the `gotHigh` path. `takeover-play-summary-branch-20260923` makes the selection follow the copy. GCC still emits only one random-hint call site across those paths (six total), so the original's duplicated random-hint fragments at lines 4770 and 4775 remain a code-generation/source-shape discrepancy. `takeover-play-summary-duplicated-20260923` tested explicit duplicated C branches; it also emitted six calls, with no reason to retain that textual duplication. The simple linear source is retained. Neither probe changes the 61/82 historical-order exact-function count; `play` remains `DIFFER`, isolated `new_game` remains exact, and `run_demo` differs.

A data-reference audit found that `function_data_refs.py` misread original `fsubrs 0x4d6da0` at `play` offset 11278 as an empty string because the float's first byte is zero. The bytes decode to `130.0f`, and the original result-panel easing at line 4699 uses that float target. The retained candidate used `136.0` as a double; `takeover-play-panel-target-20260923` corrects it to `130.0f` and emits a relocation to `0x4d6da0`. At original line 3583, the threshold is loaded by `fldl` from the double `0.01`; the candidate used `0.01f`. `takeover-play-x87-threshold-20260923` changes the literal to double. Its generated local sequence now has `fld %st(0); fldl; fxch; fucompp`, matching the original operation shape. `play` remains `DIFFER` at 17,358/17,420; no status was promoted.

The same probe's `unit.o` contains exact original literal bytes for three local arrays: `speeds[9]` from PE `0x4d6c80` (36 bytes, SHA-256 `ce457d70ab141f86558657649bb0b4406e90e3e0cbcce3fe799af706743d0c27`), `key_flag[7]` from `0x4d6c60` (28 bytes, `0b4464db7867ccfbdcabd71778a770aa610d5afdadbda9d7818988c2a1c1273d`), and `letters[31]` from `0x4d60a4` (31 bytes, `28199847c96fca4f346af25501447863e2988cbac4967e1f852c929f1f1c6e2e`). Each byte sequence occurs once in that object. This establishes literal-content agreement only; placement, relocation ownership, and object equivalence remain open.

The original results loop has only one `140.0f` reference for its panel exit at offset 11863; the candidate had a second test at loop entry and an uninitialized separate `falling` variable. Original offset 11240 loads the earlier `-0x928` counter into `%esi`, which DWARF identifies as `falling` through the result animation, and original line 4709 increments it only when it is at least 1. `takeover-play-falling-state-20260923` unifies the earlier provisional counter with `falling`, removes the extra entry test, and uses the observed conditional increment. The compiled candidate now has the same two `140.0f` references across `play` as the original (one at line 3721, one at the results exit). `play` remains `DIFFER` at 17,340/17,420 and the isolated `run_demo` still differs.

The results input waits were also incomplete. After screenshot at original offset 13086, 13091–13105 polls `KEY_F1` until release. When Tab and Shift are held, original 13052–13073 repeats `rest(2)` until `cycle_count` becomes nonzero, then reaches the panel-exit compare; the candidate instead restarted the entire frame. `takeover-play-results-waits-20260923` restores both loops. The combined historical-order unit now reports 62/82 strictly exact functions, including `new_game` and the unchanged downstream `run_demo`, with no lost match. `play` itself is still `DIFFER` at 17,501/17,420. Isolation probes show the Tab wait is responsible for the downstream match: `takeover-play-results-f1-only-20260923` leaves `run_demo` different (61/82), while `takeover-play-results-tab-only-20260923` matches it (62/82). This is an upstream source correction supported by original branches, not a cursor manipulation. Production recovery status remains unchanged.

To reproduce those isolation probes, run `python docs/attempts/game-main/play-results-isolation.py` first, then the `tu_context_probe.py` commands recorded in their JSON `edits`. The generator reads the frozen `play-results-waits-base.c` (SHA-256 `b38e63646a39d1013b667c31bb615f4390b63966da6b5a7dd6a277fd0f89588d`). The regenerated source SHA-256 hashes are `63874bba0add694a879bf0a65807b7f97d95ae428af6702080ec73dd56a9c249` (F1 only) and `110be44c2b7792ad8d09bfa52c4ba7879950a30025950fe67401ec9caea364c6` (Tab only), matching the probe records.

After restoring the waits, direct-call multiplicity showed one extra candidate `play_sound`: the replay epilogue had two textual calls for debug and nondebug paths, while original line 5011 has one physical call. Original offset 9750 branches directly from a pre-menu `debug` test to that call; after `do_replay_menu`, offset 8255 tests `recording` before reaching the same call. `takeover-play-epilogue-sound-20260923` shares one call but re-reads `debug` after the menu, which the original does not. `takeover-play-epilogue-debug-snapshot-20260923` captures the pre-menu debug value and retains a single conditional call. It has the original 16 `play_sound` calls, keeps both `new_game` and `run_demo` strictly exact in the historical-order probe, and leaves `play` `DIFFER` at 17,468/17,420. The debug snapshot is semantically relevant because the menu can execute between the two tests; it is not a register-cursor device.

Further `function_lines.py` instruction checks found three more incorrect waits. At original line 4804, offset 13118 tests `KEY_F1` and branches to itself while pressed, after the initials-entry screenshot. At line 4932, offset 13043 branches back to its `rest(2)` call at 13024 while `cycle_count==0`, then continues the frame loop. `takeover-play-name-entry-waits-20260923` restores those paths, retaining 62/82 exact functions including `new_game` and `run_demo`, with `play` still `DIFFER` at 17,480/17,420. In the gameplay screenshot path, original line 4065 at offset 4588 likewise branches to itself until F1 release, then unconditionally measures elapsed time; the prior candidate incorrectly measured only if the key was still pressed. `takeover-play-all-screenshot-waits-20260923` corrects this, with `play` 17,471/17,420 and the same exact-function set.

Original debug frame wait at offsets 6354–6425 has two paths: with Shift+Tab held, line 4361 repeatedly tests `cycle_count<=7` without a `rest` call; otherwise line 4363 repeats `rest(2)` only while `cycle_count==0`. The candidate had `rest(2)` inside the Shift+Tab loop and omitted the ordinary debug wait. `takeover-play-debug-cycle-wait-20260923` corrects the branch semantics. Its combined historical-order probe compiles `play` to 17,481/17,420, still `DIFFER`, and retains 62/82 exact functions with no losses. All three new records include full overlay and compiled-object hashes; the most recent compiled object SHA-256 is `60a86642990e255c13ac7b672729e9f87ad609801601bce78b81796888205df4`. Command shape: `python tools/tu_context_probe.py game-main src/main.c LABEL --order historical --body draw_frame=docs/attempts/game-main/draw_frame-merged.c --body play=docs/attempts/game-main/play-merged.c --focus play --focus new_game --focus run_demo --no-dumps`. The source remains an incomplete candidate, not a production match.

The original DWARF block for `hy`, `gotHigh`, `qualify[15]`, `qualifyValue[15]`, and `gameover_bmp_id` begins at historical line 4650, after the replay filename `fbuf[2048]` scope. The retained candidate had opened it before the replay work. `takeover-play-results-scope-trial-20260923` moved the block in a separate copy, then `takeover-play-results-scope-retained-20260923` verified the same change in the retained source. Candidate `speeds` now starts at `-0xac(%ebp)` and `rec_ctrl` at `-0xd0(%ebp)`, both matching the original prologue; candidate DWARF also locates `qualify` at fbreg -276 and both `qualifyValue` and `fbuf` at fbreg -2324, matching original DWARF. The frame shrank from `0xa2c` to `0xa1c`, while the original is `0x9fc`; `play` remains `DIFFER` at 17,495/17,420, and the combined unit remains 62/82 exact with no losses. The remaining 32 byte frame excess is not explained by array scope alone. At candidate line 4299, GCC spills `demo->floor-10` at `-0x9fc(%ebp)` while the original keeps it in `%ecx`, using `%ebx` for the divisor instead of candidate `%edi`. That register/spill difference is a symptom of source or TU context; do not alter the line 4299 expression merely to target the spill.

Original `play` has ten relocations to `checkMusicVoiceID` (0x4bc174). The retained candidate had only five because five pause/epilogue tests and their `voice_stop` arguments used `gameMusicVoiceID`. The source correction is in `takeover-play-check-music-voice-20260923`; that probe reaches ten candidate references to the same global. Original result-animation line 4701 tests `stars[i].intensity` before `update_particle`; the candidate called it unconditionally. `takeover-play-voice-and-results-particles-20260923` restores the guard. The original's three `hurry_y` update sites (3765, 4702, 4808) generate `lea +0x63`, `cmp 0x242`, unsigned `ja`; the retained signed test compiled to `cmp 0x1df`, signed `jg`, changing negative-value behavior. An isolated unsigned trial reproduces the original instruction sequence at all three sites, and `takeover-play-voice-particles-hurry-retained-20260923` verifies the retained candidate. Combined probe: 62/82 exact, no losses, `new_game` and unchanged `run_demo` exact; `play` remains DIFFER 17,531/17,420, `draw_frame` DIFFER 8,207/8,518. These are candidate corrections, not production recovery promotions. Next trace the duplicate key tests at original lines 4986–4987 and remaining data/reference discrepancies; keep production status unchanged.

The original unlock panel uses two distinct busy waits at 4986–4987. Offsets 15570–15598 loop while Escape, Enter, or Space remains pressed; offsets 15598–15638 then loop until one becomes pressed, branching to the replay epilogue. The candidate had only the latter wait. `takeover-play-unlock-release-wait-20260923` restores the release stage. A relocation trace found that the original calls the `screen` acquire and release vtable hooks around each of the two results shake blits (offsets 11674/11788 and 12716/12843). The candidate lacked both pairs. `takeover-play-result-screen-hooks-20260923` restores them; the candidate now has the original nine `screen` references. Original unlock-panel offset 15170 calls `makecol` before loading `gfx_driver`, then passes zero width/height to `rectfill` if the driver is null. The candidate skipped `rectfill` on that branch. `takeover-play-unlock-rectfill-20260923` restores the observed call and zero dimensions; candidate/original now each have one `gfx_driver` reference. Combined historical-order probe remains 62/82 strict, no losses, with `new_game` and `run_demo` exact; `play` is still DIFFER at 17,644/17,420 and `draw_frame` at 8,207/8,518. No production source or recovery metadata changed. Next inspect remaining original/candidate relocation and branch topology rather than code size.

The summary-message branch at original offset 11916 was reversed in the candidate. Original `recording == 0` falls through to a single NUL write at 11929, then proceeds to `init_scroller`; `recording != 0` jumps to the custom/guest/hint selection at 16813. The candidate had selected messages only when `!recording`. `takeover-play-summary-recording-20260923` corrects this substantive behavior. The combined probe still has 62/82 strict functions, with `play` DIFFER 17,648/17,420; original has seven `new_rand` call sites and two `hints` references, while the candidate still has six and one. This residual is now a source-shape/CFG question within the recording branch, not grounds to revert the corrected condition. Original `cycle_count` has eleven references and current candidate ten; the extra original read is the double volatile test in the Shift+Tab debug wait at 4361. Do not inject an arbitrary read just to match that count. Next investigate the governing source structure and remaining reference differences.

Original `play` line 4426 reloads `itrcheck` after `getGameDataXML`/`printf`/`free`: if it remains nonzero, offset 7979 branches to the shared `return 0` epilogue at 2848, bypassing end-of-play logging and replay work. The candidate fell through. `takeover-play-itrcheck-early-return-20260923` restores the second test and early return. Original line 4369 checks `itrcheck` before `rest(2)`; the candidate waited unconditionally. `takeover-play-itrcheck-tail-rest-20260923` restores that gate. Candidate/original now each have thirteen `itrcheck` references. Combined probe remains 62/82 strict with no losses; `play` is DIFFER 17,570/17,420, `draw_frame` DIFFER 8,207/8,518. The candidate now also has eleven `cycle_count` references, equal to the original; the earlier note identifying a missing debug read by count alone is superseded. Reference multiplicity is diagnostic, and no source was altered to target it. Production still has 204 strict game-function matches and no candidate promotion.

The summary path was retested after correcting its upstream `recording` gate. `play-summary-recording-split-trial.c` duplicates the guest/hint selection under `gotHigh`, as suggested by original offsets 16831–16867 and 16907–16943; GCC 4.4.1 still tail-merges it to six `new_rand` calls and one `hints` reference instead of the original seven and two. Do not repeat syntactic duplication without changing a concrete upstream type, lifetime, or control-flow hypothesis. The retained source remains the un-split branch.

The original replay filenames at lines 4578 and 4586 format `demo->no_combo_top_floor` and `demo->biggest_lost_combo` (original loads 0x5c and 0x60 from `demo`), while the candidate used the corresponding `ply[player_id]` fields. These fields are assigned earlier, but the direct data references and values under mutation differ. `takeover-play-replay-name-demo-fields-20260923` corrects both arguments. Candidate/original now each have twenty-five `demo` references; the combined TU remains 62/82 strict with no losses, `play` DIFFER 17,558/17,420 and `draw_frame` DIFFER 8,203/8,518. `ply` and `player_id` reference counts still differ and are not treated as proof of a missing operation by themselves. Production source/status unchanged.
