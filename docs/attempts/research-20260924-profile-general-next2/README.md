# profile_data_page_general bounded follow-up audit (2026-09-24)

## Result

No new body-level batch was justified from current evidence. This is an explicit local-search stop, not recovery credit. The focused current card permits edits and marks `DIFFER`, but its first mismatch is the already-studied selector CFG destination at function offset 266; it remains 1082 bytes versus 1091 historical. The current attempt ledger has 20 records. Records 8–19 already converge on offset 266, and the latest retained CFG/context study documents baseline and `!= 1` selector outcomes, all preserving the 11 exact peers in the 17-function profile CU.

## Evidence checked

- Focused card: `docs/current/functions/profile/profile_data_page_general.json` (`body_edit_allowed: true`, exact predecessor `profile_data_page_extra`, same historical/current emission position 10, first difference offset 266).
- Current focused evidence: `docs/current/function-evidence/profile/profile_data_page_general.json`.
- Attempts: `docs/attempts/game-profile/profile_data_page_general.jsonl` (20 records; early records have an entry-frame mismatch from the then-100-byte `buf`; later records return to the current 80-byte frame and converge on the selector mismatch).
- Retained investigation: `docs/attempts/research-20260924-profile-general-cfg-context/README.md` and its baseline/`!= 1` whole-CU receipts and verified-emission-neutral RTL dump.
- DWARF: nine function-scope locals (`data`, `seconds`, `minutes`, `hours`, `days`, `timeSpent[256]`, `buf[80]`, `len`, `i`); no lexical block or selector/suffix temporary evidence. Historical line mapping is coarse (selector code line 311). Calls and predecessor evidence do not identify a new source shape.

## Experiment decision

I did not compile a cosmetic selector spelling batch. Existing evidence already shows one distinct effective `!= 1` selector object, with the same first branch mismatch and no exact-function gain/loss. The retained whole-TU CFG study shows that historical and candidate branch destinations lead to corresponding singular-suffix blocks that rejoin shared formatting setup; it does not select another source form. A further source-only family would not distinguish the remaining explanations. The next discriminating work needs a concrete historical CFG/source or compiler-context mechanism from outside this body's current local evidence (for example a supported source reconstruction hypothesis or a justified predecessor/context probe).

## Strict state

No source, production ledger, card, acceptance result, or exact peer was changed. No compiler receipt was generated in this audit. Existing diagnostic outcomes remain `DIFFER`; no recovery claim is made.
